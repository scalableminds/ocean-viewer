{
	"name": "my-ocean-mock",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Mock of the MyOcean Data Portal parent app: a layer-selection UI that drives the embedded Ocean Viewer iframe over postMessage.",
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@ocean-viewer/protocol": "*",
		"react": "^18.3.1",
		"react-dom": "^18.3.1"
	},
	"devDependencies": {
		"@types/react": "^18.3.12",
		"@types/react-dom": "^18.3.1",
		"@vitejs/plugin-react": "^6.0.4"
	}
}
