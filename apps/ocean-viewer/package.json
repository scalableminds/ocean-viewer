{
	"name": "@scalableminds/ocean-viewer",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Volumetric Viewer for the MyOcean Data Portal, built on Neuroglancer. Embed as an <iframe>, drive it over postMessage.",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/scalableminds/ocean-viewer.git",
		"directory": "apps/ocean-viewer"
	},
	"files": [
		"dist",
		"examples",
		"CHANGELOG.md"
	],
	"scripts": {
		"dev": "vite --port 5174",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"typecheck": "tsc --noEmit",
		"test:smoke": "playwright test"
	},
	"dependencies": {},
	"devDependencies": {
		"@ocean-viewer/colormaps": "*",
		"@ocean-viewer/protocol": "*",
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.1.1",
		"neuroglancer": "2.41.2"
	}
}
